spring:
  application:
    name: skill-auditor-test
  jpa:
    hibernate:
      ddl-auto: none
    defer-datasource-initialization: true
  sql:
    init:
      mode: always

logging:
  level:
    root: WARN
    com.university.skillauditor: INFO